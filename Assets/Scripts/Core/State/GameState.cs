using PurrNet.StateMachine;
using UnityEngine;

public class GameState : StateNode
{
   
    public override void Enter(bool asServer)
    {
        base.Enter(asServer);
    }

    public override void Exit(bool asServer)
    {
        base.Exit(asServer);
    }
}
